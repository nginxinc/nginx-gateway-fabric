# This manifest is for testing purposes and is not the final manifest for the agent.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-agent
  template:
    metadata:
      labels:
        app: nginx-agent
    spec:
      serviceAccountName: default
      containers:
      - image: docker.io/nginx-kubernetes-gateway/agent:edge
        imagePullPolicy: IfNotPresent
        name: nginx-agent
        securityContext:
          allowPrivilegeEscalation: true
          runAsNonRoot: true
          runAsUser: 101 #nginx
          capabilities:
            drop:
            - ALL
        ports:
        - name: http
          containerPort: 8080
        - name: https
          containerPort: 8443
